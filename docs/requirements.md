System must parse C++ and Python repositories using Abstract Syntax Trees (AST) to maintain logical boundaries.

System must store code chunks and their relationships in a PostgreSQL database using pgvector.

System must utilize a modular multi-agent architecture to separate retrieval, code review, and execution logic.